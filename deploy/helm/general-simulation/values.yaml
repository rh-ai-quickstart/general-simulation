# Chart defaults (bundled with the published .tgz).
#
# Standalone installs from this repo should use the consumer values at repo root:
#   helm upgrade --install ... -f helm/values.yaml
#
# See ../../helm/README.md and ../../helm/values.yaml for the full configuration.

llm:
  mode: maas

global:
  models:
    external-model:
      enabled: true
      id: llama-scout-17b
      url: https://maas-rhdp.apps.maas.redhatworkshops.io/v1
      apiToken: ""

api:
  models:
    generation: external-model/llama-scout-17b

ingestion:
  models:
    generation: external-model/llama-scout-17b
